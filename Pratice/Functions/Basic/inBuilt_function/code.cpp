#include <iostream>
#include<cmath>
using namespace std;
int main()
{
    int a = 4, b = 3;
    cout << "Max is " << max(a, b)<<endl;
    cout << "Min is " << min(a, b)<<endl;
    cout<<"The SQRT value of "<<a<<" is "<<sqrt(a)<<endl;
    cout << "Power is " << pow(a, b)<<endl;
}